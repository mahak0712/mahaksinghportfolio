<!-- <div class="card">
  <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
  <p class="card-text">
    <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span> <%= portfolio_item.subtitle %>
  </p>
</div>  -->

   <li class="li" data-id= "<%= portfolio_item.id %>">
            <div class="hovereffect" >

              <figure class="meal-photo">
               <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil?%>
              </figure>
              <div class="overlay">
                <h2><%=portfolio_item.title %></h2>
                <%= link_to portfolio_item.title, portfolio_show_path(portfolio_item)  ,class: "info" %>
                

              </div>
            </div>

          </li>       





























<!-- <p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>

<%= link_to "Edit", edit_portfolio_path(portfolio_item.id) if logged_in?(:site_admin)  %>
<%= link_to 'Delete Portfolio Item', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)  %> -->
